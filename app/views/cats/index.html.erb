
<div class="container">
  <h1>All Cats</h1>
  <div class="row">
      <% @cats.each do |cat| %>
    <div class="col-xs-12 col-sm-4">
 <!--  <%= link_to 'Add a cat', new_owner_cat_path %> -->
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path(cat.photo, height: 300, width: 400, crop: :fill) %>');">
            <div class="card-category"><%= cat.price_per_day %> € / day</div>
            <div class="card-description">
              <h2><%= cat.name %></h2>
              <p><%= cat.breed %></p>
            </div>
        </div>
    </div>
  <% end %>
  </div>
</div>
